Things to do to reeach Roadmap-0
 
 The changes that have not been pushed:
    - Now we do not have a common_requirements.txt file
    - The common requirements are copied into each services's requirements.txt file
    - We need to build a venv inside each service for local pylinting
    - We need to code the lightining datamodule for MNIST
    - We need to code the a general transformer
    - We need to code ViT and make it run on MNIST
